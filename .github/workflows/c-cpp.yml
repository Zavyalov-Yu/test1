name: C/C++ Build
on: [workflow_dispatch]
  #push:
  #  branches: [ "master" ]
  #pull_request:
  #  branches: [ "master" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
            - name: checkout
            - uses: actions/checkout@v3
            - name: ls -a
              run: ls -a
